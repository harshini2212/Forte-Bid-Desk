# Optional. The whole demo runs offline with no key (cached estimates + a
# deterministic teacher). Set a key to route the ambiguous scope lines and
# Forte AI questions through live Claude.
ANTHROPIC_API_KEY=
# Drip the live stream (seconds between events) so a demo feels live. 0 = full speed.
RUN_PACE=0.28
